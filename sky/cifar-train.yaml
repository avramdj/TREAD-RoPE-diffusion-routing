resources:
  cloud: runpod
  accelerators: A100-80GB:1

workdir: .

secrets:
  WANDB_API_KEY: null

setup: |
  uv sync

run: |
  DISABLE_JAX_TYPING=1 uv run python examples/train_cifar.py --epochs 1000 --batch-size 1024